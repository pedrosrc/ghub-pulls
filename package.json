{
  "name": "ghub-pulls",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension to track GitHub Pull Requests that need your attention",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.4.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
